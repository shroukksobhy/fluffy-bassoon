const HeroSection = () => {
    return (
        <section className="hero">
            <div className="container">
                <h1>Unlock Your Potential with Our Course</h1>
                <p>Learn valuable skills from industry experts and take your career to the next level.</p>
                <button>Enroll Now</button>
            </div>
        </section>
    );
};

export default HeroSection;
